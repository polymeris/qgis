FILE(GLOB UI_FILES dialog.ui panel.ui)
PYQT4_WRAP_UI(PYUI_FILES ${UI_FILES})

ADD_CUSTOM_TARGET(processing_gui ALL DEPENDS ${PYUI_FILES})

SET (INSTALL_FILES
    __init__.py
    ${PYUI_FILES}
    )

INSTALL(FILES
    ${INSTALL_FILES}
    DESTINATION
    ${QGIS_PROCESSING_FRAMEWORK_DIR}/gui)

