SUBDIRS(gui)

SET (QGIS_PROCESSING_FRAMEWORK_DIR ${QGIS_PYTHON_DIR}/processing)

PYTHON_INSTALL(__init__.py ${QGIS_PROCESSING_FRAMEWORK_DIR})
PYTHON_INSTALL(parameters.py ${QGIS_PROCESSING_FRAMEWORK_DIR})
